apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ template "app.chartname" . }}-configmap"
  namespace: {{ .Release.Namespace }}
data:
  signatory.conf: |
        {
            "proofConfig": {
                "issuerDid": "",
                "proofType": "LD_PROOF",
                "credentialsEndpoint": {{ .Values.config.signatory.credentialsEndpoint | quote }}
            },
            "templatesFolder": "vc-templates-runtime"
        }